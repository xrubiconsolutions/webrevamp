import { support } from "../../../utils/data";
import BannerSlide from "../../BannerSlide";

const Support = () => {
  return <BannerSlide title="PAKAM IS SUPPORTED BY." imgUrl={support} />;
};

export default Support;
